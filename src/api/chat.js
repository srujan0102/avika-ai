export async function sendMessageToChatbot(message) {
  // placeholder: call backend or third-party API
  return new Promise((resolve) => {
    setTimeout(() => resolve({ reply: 'This is a placeholder reply.' }), 300);
  });
}
